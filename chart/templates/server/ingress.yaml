{{- if and (include "ggbridge.server.hostname" .) .Values.server.ingress.enabled }}
  {{- $fullname := include "ggbridge.fullname" . }}
  {{- $serverFullname := include "ggbridge.server.fullname" . }}

{{- if and (eq .Values.server.ingress.controller "traefik") .Values.server.tls.enabled }}
---
apiVersion: traefik.io/v1alpha1
kind: TLSOption
metadata:
  name: {{ $serverFullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ggbridge.labels" . | nindent 4 }}
    {{- include "ggbridge.server.labels" . | nindent 4 }}
spec:
  {{- if eq (lower .Values.server.tls.mode) "mutual" }}
  clientAuth:
    secretNames:
      - {{ printf "%s-crt" $serverFullname }}
    clientAuthType: RequireAndVerifyClientCert
  {{- end }}
  minVersion: VersionTLS12
{{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ $serverFullname }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "ggbridge.labels" . | nindent 4 }}
    {{- include "ggbridge.server.labels" . | nindent 4 }}
  annotations: {{ include "ggbridge.server.ingress.annotations" . | nindent 4 }}
spec:
  ingressClassName: {{ .Values.server.ingress.className | quote }}
  rules:
    - host: {{ include "ggbridge.server.hostname" . | quote }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ $serverFullname }}
                port:
                  number: {{ $.Values.server.service.ports.ws.port }}
          {{- range $index := until (include "ggbridge.server.deploymentCount" $ | int) }}
            {{- $serverFullname := include "ggbridge.server.fullname" $ }}
            {{- $indexServerFullname := printf "%s-%d" $serverFullname ($index | int) }}
          - path: {{ printf "/%d" ($index | int) }}
            pathType: Prefix
            backend:
              service:
                name: {{ $indexServerFullname }}
                port:
                  number: {{ $.Values.server.service.ports.ws.port }}
          {{- end }}
  {{- if .Values.server.tls.enabled }}
  tls:
    - hosts:
        - {{ include "ggbridge.server.hostname" . | quote }}
      {{- if .Values.server.tls.existingSecret }}
      secretName: {{ .Values.server.tls.existingSecret }}
      {{- else }}
      secretName: {{ printf "%s-crt" $serverFullname }}
      {{- end }}
  {{- end }}

{{- end }}
